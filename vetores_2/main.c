#include <stdio.h>
#include <stdlib.h>

int main(){
    int i,vet[5] = {0,7,2,3,4};
    for(i = 0;i < 5;i++){
        printf("Vetor[%d] = %d\n",i, vet[i]);
    }  
    
    system("pause");
    return 0;
}
